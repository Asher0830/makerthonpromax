# 惜食整合系統 — 完整技術規格書 v1.0

> **文件版本**：v1.0  
> **最後更新**：2026-05-29  
> **開發策略**：本地優先開發 → Cloudflare 部署  
> **MVP 範圍**：地圖導購 + 扭蛋機台（不含寵物養成系統）

---

## 目錄

1. [專案概述](#1-專案概述)
2. [系統架構](#2-系統架構)
3. [開發環境與部署策略](#3-開發環境與部署策略)
4. [資料庫 Schema](#4-資料庫-schema)
5. [API 規格](#5-api-規格)
6. [MQTT 通訊規格](#6-mqtt-通訊規格)
7. [狀態機定義](#7-狀態機定義)
8. [前端規格 — 平板端 (Tablet Kiosk)](#8-前端規格--平板端-tablet-kiosk)
9. [前端規格 — 手機端 (Mobile Web)](#9-前端規格--手機端-mobile-web)
10. [核心業務流程](#10-核心業務流程)
11. [安全與防呆機制](#11-安全與防呆機制)
12. [未來擴充](#12-未來擴充)

---

## 1. 專案概述

### 1.1 產品定位

一款結合 **O2O 地圖導購**與 **IoT 實體盲盒機台**的惜食救援系統。讓即期食品（便當、蔬菜、麵包等）透過趣味化的扭蛋機制被消費者取得，減少食物浪費。

### 1.2 兩條核心路徑

| 路徑 | 說明 | 涉及元件 |
|------|------|----------|
| **路徑一：地圖導購** | 店家在平台上架即期品，消費者透過地圖找到附近店家，線上付款後到店取餐 | 手機 Web（店家端 + 消費者端）、後端 API、資料庫 |
| **路徑二：扭蛋機台** | 店家將食物放入機台，消費者在機台前付款、選擇條件、轉旋鈕抽獎取餐 | 平板 Web、手機 Web（付款）、後端 API、ESP32 硬體、MQTT |

### 1.3 MVP 範圍（本版本）

**包含：**
- ✅ 店家/消費者帳號系統（Email + 密碼）
- ✅ 路徑一：地圖導購完整流程
- ✅ 路徑二：扭蛋機台完整流程
- ✅ 過敏原篩選機制
- ✅ 基礎點數累計
- ✅ Mock 假金流（演示用）

**不包含：**
- ❌ 虛擬寵物養成系統（後續迭代）
- ❌ 真實金流串接（後續替換 mock）
- ❌ 推播通知

---

## 2. 系統架構

### 2.1 架構總覽

```
┌──────────────────────────────────────────────────────────────────┐
│                        使用者裝置層                               │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────────────────┐ │
│  │  手機 Web   │  │  平板 Kiosk │  │    ESP32 邊緣節點        │ │
│  │  (消費者/   │  │  (機台展示) │  │  (旋鈕/門鎖/溫控)       │ │
│  │   店家)     │  │             │  │  ⚠ 不在本文件範圍內     │ │
│  └──────┬──────┘  └──────┬──────┘  └────────┬─────────────────┘ │
└─────────┼────────────────┼─────────────────┼────────────────────┘
          │ HTTPS          │ HTTPS (Polling) │ HTTPS POST (觸發)
          ▼                ▼                 │    ▲ MQTT (指令)
┌─────────────────────────────────────┐      │    │
│         應用伺服器層                 │      │    │
│  ┌─────────────────────────────┐    │      │    │
│  │   Hono (Node.js / Workers)  │◄───┼──────┘    │
│  │   REST API + 靜態檔案       │    │           │
│  └──────────┬──────────────────┘    │           │
│             │                       │           │
│  ┌──────────▼──────────────────┐    │    ┌──────┴──────┐
│  │   SQLite / Cloudflare D1    │    │    │ EMQX Cloud  │
│  │   (資料庫)                  │    │    │ (MQTT 橋樑) │
│  └─────────────────────────────┘    │    └─────────────┘
└─────────────────────────────────────┘
```

### 2.2 四大核心單位

#### 2.2.1 後端 API 伺服器

| 面向 | 本地開發 | 雲端部署 |
|------|----------|----------|
| 框架 | Hono + `@hono/node-server` | Hono + Cloudflare Workers |
| 資料庫 | `better-sqlite3`（本地 .db 檔案） | Cloudflare D1 |
| 靜態檔案 | Hono `serveStatic` middleware | Cloudflare Pages 或 Workers Sites |
| MQTT 發布 | 本地 mock（console.log）或直連 EMQX | HTTP POST → EMQX REST API |

**職責：**
- 處理所有 HTTP API 請求
- 帳號認證（JWT）
- 盲盒抽獎演算法
- 訂單狀態機管理
- 庫存管理與預留
- 透過 EMQX HTTP API 下發 MQTT 開門指令

#### 2.2.2 ESP32 邊緣節點（⚠ 不在本文件範圍）

由硬體團隊負責，介面定義見 [Section 5](#5-api-規格) 和 [Section 6](#6-mqtt-通訊規格)。

#### 2.2.3 平板端 (Tablet Kiosk Web)

- 全螢幕 SPA，運行在機台上的平板瀏覽器中
- 透過 Polling 同步機台狀態（閒置時 5 秒間隔，`WAITING_FOR_TRIGGER` 時 500ms 間隔）
- 職責：展示盲盒池資訊、過敏原選擇、產生付款 QR Code、播放扭蛋動畫、店家配對 QR Code

#### 2.2.4 手機端 (Mobile Web)

- 響應式網頁，不需安裝 App
- 雙模式：消費者模式 / 店家模式
- 消費者模式：地圖導購、線上付款、掃碼付款（機台場景）、查看點數
- 店家模式：上架商品、掃碼配對機台、管理艙位

---

## 3. 開發環境與部署策略

### 3.1 本地開發環境

```
技術棧：
├── 執行環境：Node.js 20+
├── 後端框架：Hono 4.x
├── 資料庫：better-sqlite3（本地 SQLite）
├── 前端：Vanilla HTML/CSS/JS（無框架）
├── 套件管理：npm
└── 開發指令：npm run dev
```

**本地專案結構：**
```
foodd/
├── package.json
├── server.js                    # 本地 Node.js 入口
├── src/
│   ├── app.js                   # Hono app 定義（跨平台共用）
│   ├── routes/
│   │   ├── auth.js              # 帳號認證 API
│   │   ├── products.js          # 商品管理 API
│   │   ├── stores.js            # 店家 API
│   │   ├── orders.js            # 訂單 API
│   │   ├── machines.js          # 機台 API（含觸發抽獎）
│   │   └── payment.js           # Mock 金流 API
│   ├── middleware/
│   │   └── auth.js              # JWT 驗證中間件
│   ├── services/
│   │   ├── gacha.js             # 盲盒抽獎演算法
│   │   ├── inventory.js         # 庫存管理（含預留邏輯）
│   │   └── mqtt.js              # MQTT 發布服務（可 mock）
│   ├── db/
│   │   ├── schema.sql           # 資料表定義
│   │   ├── seed.sql             # 測試種子資料
│   │   └── connection.js        # 資料庫連線抽象層
│   └── utils/
│       ├── jwt.js               # JWT 工具
│       └── errors.js            # 錯誤處理
├── public/
│   ├── tablet/                  # 平板端前端
│   │   ├── index.html
│   │   ├── css/
│   │   │   └── tablet.css
│   │   └── js/
│   │       ├── app.js           # 主程式 + 路由
│   │       ├── polling.js       # 狀態輪詢
│   │       ├── gacha-animation.js  # 扭蛋動畫
│   │       └── qrcode.js        # QR Code 產生
│   └── mobile/                  # 手機端前端
│       ├── index.html
│       ├── manifest.json        # PWA manifest
│       ├── sw.js                # Service Worker
│       ├── css/
│       │   └── mobile.css
│       └── js/
│           ├── app.js           # 主程式 + Hash Router
│           ├── views/
│           │   ├── consumer-map.js    # 消費者地圖頁
│           │   ├── consumer-store.js  # 店家詳情頁
│           │   ├── consumer-pay.js    # 付款頁
│           │   ├── consumer-points.js # 點數頁
│           │   ├── store-dashboard.js # 店家主頁
│           │   ├── store-add-item.js  # 上架商品頁
│           │   ├── store-pair.js      # 機台配對頁
│           │   ├── login.js           # 登入頁
│           │   └── register.js        # 註冊頁
│           ├── api.js            # API 呼叫封裝
│           └── auth.js           # 登入狀態管理
├── wrangler.toml                # Cloudflare 部署設定（後續用）
└── README.md
```

### 3.2 部署遷移路徑

```
階段 1（現在）：本地 Node.js + SQLite
    ↓ 流程跑通、演示完成
階段 2：部署到 Cloudflare Workers + D1
    ↓ 只需替換 server.js 入口 + db/connection.js 抽象層
階段 3（未來）：接入真實金流 + 寵物系統
```

遷移時需要修改的檔案僅：
- `server.js` → 替換為 Workers 入口 `worker.js`
- `db/connection.js` → 從 `better-sqlite3` 切換為 D1 binding
- `services/mqtt.js` → 從 mock 切換為真實 EMQX HTTP API

---

## 4. 資料庫 Schema

### 4.1 ER 關係圖

```
┌──────────┐     ┌───────────┐     ┌─────────────┐
│  users   │────<│  stores   │────<│  products   │
└──────────┘     └─────┬─────┘     └──────┬──────┘
                       │                  │
                       │           ┌──────▼──────┐
                 ┌─────▼─────┐    │  product_   │
                 │ machines  │    │  allergens  │
                 └─────┬─────┘    └─────────────┘
                       │
                 ┌─────▼─────┐     ┌─────────────┐
                 │compartments│────<│   orders    │
                 └───────────┘     └─────────────┘
```

### 4.2 資料表定義

#### `users` — 使用者帳號

```sql
CREATE TABLE users (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    email       TEXT NOT NULL UNIQUE,
    password    TEXT NOT NULL,          -- bcrypt hash
    role        TEXT NOT NULL CHECK(role IN ('consumer', 'store_owner', 'admin')),
    name        TEXT NOT NULL,
    points      INTEGER NOT NULL DEFAULT 0,  -- 累計點數
    created_at  TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
```

#### `stores` — 店家資料

```sql
CREATE TABLE stores (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id     INTEGER NOT NULL REFERENCES users(id),
    name        TEXT NOT NULL,           -- 店家名稱
    description TEXT,                    -- 店家描述
    address     TEXT NOT NULL,           -- 實體地址
    latitude    REAL NOT NULL,           -- 緯度
    longitude   REAL NOT NULL,           -- 經度
    phone       TEXT,
    is_active   INTEGER NOT NULL DEFAULT 1,  -- 是否營業中
    created_at  TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
```

#### `machines` — 機台

```sql
CREATE TABLE machines (
    id              TEXT PRIMARY KEY,       -- 例：MAC_01A2B3
    name            TEXT NOT NULL,          -- 機台顯示名稱
    location_desc   TEXT,                   -- 擺放位置描述
    latitude        REAL,
    longitude       REAL,
    total_compartments INTEGER NOT NULL DEFAULT 6,   -- 總艙位數
    status          TEXT NOT NULL DEFAULT 'IDLE'
                    CHECK(status IN ('IDLE', 'WAITING_FOR_TRIGGER', 'DISPENSING', 'ERROR', 'MAINTENANCE')),
    active_order_id INTEGER REFERENCES orders(id),   -- 當前進行中的訂單（單槽模型）
    secret_key      TEXT NOT NULL,          -- ESP32 認證金鑰
    last_telemetry  TEXT,                   -- 最後一次溫濕度 JSON
    last_seen_at    TEXT,                   -- 最後心跳時間
    created_at      TEXT NOT NULL DEFAULT (datetime('now'))
);
```

#### `compartments` — 機台艙位

```sql
CREATE TABLE compartments (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    machine_id  TEXT NOT NULL REFERENCES machines(id),
    index_num   INTEGER NOT NULL,          -- 艙位編號（1~N）
    status      TEXT NOT NULL DEFAULT 'EMPTY'
                CHECK(status IN ('EMPTY', 'STOCKED', 'RESERVED', 'DISPENSED')),
    product_id  INTEGER REFERENCES products(id),
    stocked_at  TEXT,                      -- 放入時間
    stocked_by  INTEGER REFERENCES users(id),  -- 哪個店家放的
    UNIQUE(machine_id, index_num)
);
```

#### `products` — 商品

```sql
CREATE TABLE products (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    store_id        INTEGER NOT NULL REFERENCES stores(id),
    name            TEXT NOT NULL,          -- 商品名稱
    category        TEXT NOT NULL CHECK(category IN ('bento', 'bread', 'vegetable', 'other')),
    original_price  REAL NOT NULL,          -- 原價
    selling_price   REAL NOT NULL,          -- 惜食價
    description     TEXT,
    source          TEXT NOT NULL CHECK(source IN ('map', 'machine')),  -- 上架路徑
    status          TEXT NOT NULL DEFAULT 'AVAILABLE'
                    CHECK(status IN ('AVAILABLE', 'RESERVED', 'SOLD', 'EXPIRED')),
    expires_at      TEXT,                   -- 有效期限
    created_at      TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at      TEXT NOT NULL DEFAULT (datetime('now'))
);
```

#### `product_allergens` — 商品過敏原標籤

```sql
-- 過敏原類型定義
-- 肉類：pork（豬）、beef（牛）、chicken（雞）、duck（鴨）、lamb（羊）、seafood（海鮮）
-- 其他：egg（蛋）、milk（乳）、peanut（花生）、treenut（堅果）、wheat（小麥）、soy（大豆）、sesame（芝麻）

CREATE TABLE product_allergens (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    product_id  INTEGER NOT NULL REFERENCES products(id),
    allergen    TEXT NOT NULL,
    UNIQUE(product_id, allergen)
);
```

#### `orders` — 訂單

```sql
CREATE TABLE orders (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id         INTEGER NOT NULL REFERENCES users(id),
    order_type      TEXT NOT NULL CHECK(order_type IN ('map_purchase', 'machine_gacha')),

    -- 路徑一（地圖導購）欄位
    store_id        INTEGER REFERENCES stores(id),
    product_id      INTEGER REFERENCES products(id),      -- 地圖導購：指定商品

    -- 路徑二（機台扭蛋）欄位
    machine_id      TEXT REFERENCES machines(id),
    compartment_id  INTEGER REFERENCES compartments(id),   -- 抽中的艙位
    excluded_allergens TEXT,                                -- JSON array，例：["pork","seafood"]
    pool_size       INTEGER,                                -- 抽獎時池中商品數
    pool_avg_price  REAL,                                   -- 篩選後子池均價

    -- 共用欄位
    amount          REAL NOT NULL,                          -- 實付金額
    status          TEXT NOT NULL DEFAULT 'PENDING'
                    CHECK(status IN (
                        'PENDING',                -- 建立中
                        'PAID',                   -- 已付款（路徑一結束狀態前置）
                        'WAITING_FOR_TRIGGER',    -- 等待轉旋鈕（路徑二）
                        'DISPENSING',             -- 出餐中
                        'COMPLETED',              -- 完成
                        'TIMEOUT_REFUNDED',       -- 超時退款
                        'CANCELLED'               -- 取消
                    )),
    points_earned   INTEGER NOT NULL DEFAULT 0,
    version         INTEGER NOT NULL DEFAULT 1,            -- 樂觀鎖版本號
    paid_at         TEXT,
    completed_at    TEXT,
    timeout_at      TEXT,                                   -- WAITING 狀態的超時時間
    created_at      TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at      TEXT NOT NULL DEFAULT (datetime('now'))
);
```

#### `sessions` — 平板配對 Session

```sql
CREATE TABLE sessions (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    token       TEXT NOT NULL UNIQUE,       -- URL Token
    machine_id  TEXT NOT NULL REFERENCES machines(id),
    store_id    INTEGER REFERENCES stores(id),   -- 配對成功後填入
    status      TEXT NOT NULL DEFAULT 'PENDING'
                CHECK(status IN ('PENDING', 'PAIRED', 'EXPIRED')),
    expires_at  TEXT NOT NULL,
    created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
```

### 4.3 索引

```sql
CREATE INDEX idx_products_store ON products(store_id);
CREATE INDEX idx_products_status ON products(status);
CREATE INDEX idx_products_source ON products(source);
CREATE INDEX idx_compartments_machine ON compartments(machine_id);
CREATE INDEX idx_orders_user ON orders(user_id);
CREATE INDEX idx_orders_machine ON orders(machine_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_sessions_token ON sessions(token);
```

---

## 5. API 規格

### 5.1 基本設定

- **Base URL**：`http://localhost:3000/api/v1`（本地）或 `https://api.yourdomain.com/v1`（部署）
- **認證方式**：JWT Bearer Token（`Authorization: Bearer <token>`）
- **回應格式**：JSON
- **錯誤格式**：
```json
{
    "success": false,
    "error": {
        "code": "ORDER_ALREADY_COMPLETED",
        "message": "此訂單已完成，無法重複觸發"
    }
}
```

### 5.2 認證相關 API

#### `POST /api/v1/auth/register` — 註冊

```
Request Body:
{
    "email": "user@example.com",
    "password": "mypassword",
    "name": "王小明",
    "role": "consumer"              // "consumer" | "store_owner"
}

Response 201:
{
    "success": true,
    "data": {
        "user": { "id": 1, "email": "...", "name": "...", "role": "consumer" },
        "token": "eyJhbGci..."
    }
}
```

#### `POST /api/v1/auth/login` — 登入

```
Request Body:
{
    "email": "user@example.com",
    "password": "mypassword"
}

Response 200:
{
    "success": true,
    "data": {
        "user": { "id": 1, "email": "...", "name": "...", "role": "...", "points": 120 },
        "token": "eyJhbGci..."
    }
}
```

### 5.3 店家 API（需 `store_owner` 身分）

#### `POST /api/v1/stores` — 建立店家檔案

```
Request Body:
{
    "name": "阿美便當",
    "address": "台北市大安區忠孝東路100號",
    "latitude": 25.0418,
    "longitude": 121.5437,
    "phone": "02-12345678",
    "description": "每日新鮮現做便當"
}
```

#### `GET /api/v1/stores/me` — 取得自己的店家資訊

#### `PUT /api/v1/stores/me` — 更新店家資訊

### 5.4 商品 API

#### `POST /api/v1/products` — 上架商品（店家）

```
Request Body:
{
    "name": "雞腿便當",
    "category": "bento",
    "original_price": 100,
    "selling_price": 60,
    "source": "map",                        // "map" | "machine"
    "description": "今日特餐，雞腿+三菜",
    "allergens": ["chicken", "egg", "soy"],  // 過敏原
    "expires_at": "2026-05-29T20:00:00+08:00"
}
```

#### `GET /api/v1/products?source=map&lat=25.04&lng=121.54&radius=3` — 附近商品（消費者地圖用）

```
Response 200:
{
    "success": true,
    "data": {
        "products": [
            {
                "id": 1,
                "name": "雞腿便當",
                "selling_price": 60,
                "store": { "id": 1, "name": "阿美便當", "lat": 25.04, "lng": 121.54 },
                "allergens": ["chicken", "egg", "soy"],
                "distance_km": 0.3
            }
        ]
    }
}
```

#### `GET /api/v1/products/:id` — 商品詳情

### 5.5 機台 API

#### `GET /api/v1/machines/:machineId/status` — 取得機台狀態（平板 Polling 用）

```
Response 200:
{
    "success": true,
    "data": {
        "machine_id": "MAC_01A2B3",
        "status": "WAITING_FOR_TRIGGER",     // IDLE | WAITING_FOR_TRIGGER | DISPENSING
        "active_order": {
            "id": 42,
            "status": "WAITING_FOR_TRIGGER",
            "timeout_at": "2026-05-29T16:25:00Z"
        },
        "compartments": [
            { "index": 1, "status": "STOCKED", "product_name": "排骨便當", "category": "bento" },
            { "index": 2, "status": "STOCKED", "product_name": "菠蘿麵包", "category": "bread" },
            { "index": 3, "status": "EMPTY" },
            { "index": 4, "status": "STOCKED", "product_name": "鮪魚飯糰", "category": "bento" },
            { "index": 5, "status": "EMPTY" },
            { "index": 6, "status": "STOCKED", "product_name": "有機蔬菜包", "category": "vegetable" }
        ],
        "pool_summary": {
            "total_stocked": 4,
            "avg_price": 55.0,
            "categories": ["bento", "bread", "vegetable"]
        },
        "last_telemetry": {
            "temperature": 15.2,
            "humidity": 60.5
        }
    }
}
```

#### `POST /api/v1/machines/:machineId/gacha/start` — 開始扭蛋流程（消費者付款前）

```
Request Body:
{
    "excluded_allergens": ["pork", "seafood"]   // 不想吃的
}

Response 200:
{
    "success": true,
    "data": {
        "pool_size": 3,                    // 篩選後剩餘商品數
        "pool_avg_price": 53.33,           // 篩選後均價（= 付款金額）
        "available_categories": ["bento", "bread"],
        "payment_url": "/mobile/pay?order_id=42&amount=53.33"
    }
}

Response 409 (機台忙碌):
{
    "success": false,
    "error": {
        "code": "MACHINE_BUSY",
        "message": "前一位消費者尚未完成，請稍候"
    }
}

Response 400 (池空):
{
    "success": false,
    "error": {
        "code": "POOL_EMPTY",
        "message": "篩選後無可用商品，請放寬過敏原條件"
    }
}
```

#### `POST /api/v1/machines/:machineId/gacha/pay` — 確認付款（Mock）

```
Request Body:
{
    "order_id": 42
}

Response 200:
{
    "success": true,
    "data": {
        "order_id": 42,
        "status": "WAITING_FOR_TRIGGER",
        "timeout_at": "2026-05-29T16:25:00Z",    // 5 分鐘後超時
        "message": "付款成功！請轉動機台旋鈕"
    }
}
```

#### `POST /api/v1/machines/trigger` — ESP32 觸發抽獎 ⚡

```
Request Body (來自 ESP32):
{
    "machine_id": "MAC_01A2B3",
    "action": "KNOB_TURNED",
    "secret_key": "esp32_auth_token"
}

Response 200 (抽獎成功):
{
    "success": true,
    "data": {
        "order_id": 42,
        "won_compartment": 4,
        "won_product": {
            "name": "鮪魚飯糰",
            "category": "bento"
        },
        "points_earned": 10,
        "door_command_sent": true
    }
}

Response 403 (無等待中的訂單):
{
    "success": false,
    "error": {
        "code": "NO_WAITING_ORDER",
        "message": "目前沒有等待觸發的訂單"
    }
}
```

#### `GET /api/v1/machines/:machineId/latest-result` — 取得最新抽獎結果（平板 Polling 用）

```
Response 200:
{
    "success": true,
    "data": {
        "order_id": 42,
        "status": "DISPENSING",
        "won_compartment": 4,
        "won_product": {
            "name": "鮪魚飯糰",
            "category": "bento"
        },
        "animation_played": false    // 平板讀取後設為 true，避免重複播放
    }
}
```

### 5.6 機台配對 API（店家 ↔ 機台）

#### `POST /api/v1/machines/:machineId/pair/generate` — 產生配對 Token（平板呼叫）

```
Response 200:
{
    "success": true,
    "data": {
        "pair_url": "https://localhost:3000/mobile/#/pair?token=abc123def",
        "token": "abc123def",
        "expires_at": "2026-05-29T16:30:00Z"    // 10 分鐘有效
    }
}
```

#### `POST /api/v1/machines/pair/confirm` — 確認配對（店家手機呼叫）

```
Request Body:
{
    "token": "abc123def"
}

Response 200:
{
    "success": true,
    "data": {
        "machine_id": "MAC_01A2B3",
        "machine_name": "大安站 1 號機",
        "compartments": [
            { "index": 1, "status": "EMPTY" },
            { "index": 2, "status": "STOCKED" },
            ...
        ]
    }
}
```

#### `POST /api/v1/machines/:machineId/compartments/:index/stock` — 放餐入艙（店家）

```
Request Body:
{
    "product_id": 15          // 已上架的商品 ID
}

→ 後端同時發送 MQTT 開門指令讓 ESP32 解鎖該艙位
```

### 5.7 訂單 API（路徑一：地圖導購）

#### `POST /api/v1/orders/map` — 地圖導購下單

```
Request Body:
{
    "product_id": 15
}

Response 201:
{
    "success": true,
    "data": {
        "order_id": 55,
        "product_name": "雞腿便當",
        "store_name": "阿美便當",
        "amount": 60,
        "status": "PAID",
        "pickup_code": "A55B",          // 4 碼取餐碼
        "points_earned": 6,
        "message": "請至店家出示取餐碼"
    }
}
```

#### `GET /api/v1/orders/me` — 我的訂單列表

#### `POST /api/v1/orders/:id/complete` — 店家確認核銷

```
Request Body:
{
    "pickup_code": "A55B"
}
```

### 5.8 超時自動退款（排程任務）

本地開發用 `setInterval` 每 30 秒掃描一次；Cloudflare 部署用 Cron Trigger。

```sql
-- 找出超時訂單
UPDATE orders
SET status = 'TIMEOUT_REFUNDED', updated_at = datetime('now')
WHERE status = 'WAITING_FOR_TRIGGER'
  AND timeout_at < datetime('now');

-- 同時重設機台狀態
UPDATE machines
SET status = 'IDLE', active_order_id = NULL
WHERE active_order_id IN (SELECT id FROM orders WHERE status = 'TIMEOUT_REFUNDED');
```

---

## 6. MQTT 通訊規格

### 6.1 通訊拓撲

```
Cloudflare Workers ──HTTP POST──► EMQX REST API ──MQTT──► ESP32
ESP32 ──MQTT Publish──► EMQX ──Webhook──► Cloudflare Workers（遙測資料）
```

### 6.2 Topic 命名規範

| Topic | 方向 | 說明 |
|-------|------|------|
| `v1/machines/{id}/control` | Server → ESP32 | 開門指令 |
| `v1/machines/{id}/telemetry` | ESP32 → Server | 溫濕度遙測（每 60 秒） |
| `v1/machines/{id}/heartbeat` | ESP32 → Server | 心跳（每 30 秒） |

### 6.3 開門指令 Payload

```json
{
    "action": "OPEN",
    "door_index": 4,
    "pwm_ms": 1500,
    "auto_close_sec": 3,
    "request_id": "ord_42_1716990300"
}
```

### 6.4 遙測 Payload

```json
{
    "temperature": 15.2,
    "humidity": 60.5,
    "hardware_status": {
        "cooling_relay": true,
        "heating_relay": false
    },
    "timestamp": "2026-05-29T16:20:00Z"
}
```

### 6.5 本地開發 MQTT Mock

本地開發時，`services/mqtt.js` 不實際連線 EMQX，改為：
- `console.log` 輸出指令內容
- 直接回傳成功（模擬門已開）
- 可透過環境變數 `MQTT_MODE=live` 切換為真實連線

---

## 7. 狀態機定義

### 7.1 機台狀態機

```
                 ┌──────────┐
                 │   IDLE   │ ◄─── 超時退款 / 取餐完成
                 └────┬─────┘
                      │ 消費者付款成功
                      ▼
            ┌─────────────────────┐
            │ WAITING_FOR_TRIGGER │ ──── 5 分鐘超時 ──► IDLE（退款）
            └─────────┬───────────┘
                      │ ESP32 轉旋鈕觸發
                      ▼
               ┌──────────────┐
               │  DISPENSING   │
               └──────┬───────┘
                      │ 門已開 / 取餐完成
                      ▼
                 ┌──────────┐
                 │   IDLE   │
                 └──────────┘
```

### 7.2 訂單狀態機

```
PENDING ──付款──► PAID（路徑一完成付款）
                    │
                    ├── 核銷取餐 ──► COMPLETED
                    └── 取消 ──► CANCELLED

PENDING ──付款──► WAITING_FOR_TRIGGER（路徑二等待旋鈕）
                    │
                    ├── 旋鈕觸發 ──► DISPENSING ──► COMPLETED
                    └── 5min 超時 ──► TIMEOUT_REFUNDED
```

### 7.3 艙位狀態機

```
EMPTY ──店家放餐──► STOCKED ──抽中──► RESERVED ──門開取餐──► DISPENSED ──清理──► EMPTY
```

---

## 8. 前端規格 — 平板端 (Tablet Kiosk)

### 8.1 設計原則

- **全螢幕 Kiosk 模式**：隱藏瀏覽器 UI，防止誤觸返回
- **大字體、大按鈕**：適合站著操作
- **深色主題**：搭配機台外觀，營造科技感
- **動畫豐富**：扭蛋開獎動畫是核心體驗亮點

### 8.2 頁面 / 狀態

| 狀態 | 畫面 | 觸發條件 |
|------|------|----------|
| **待機 (IDLE)** | 顯示機台歡迎畫面、目前有幾種商品、環境溫度 | 機台閒置 |
| **店家配對** | 顯示 QR Code 供店家掃描 | 店家按下配對按鈕 |
| **選擇條件** | 過敏原剔除勾選（不吃豬/牛/海鮮…）、顯示篩選後均價 | 消費者點擊「開始扭蛋」 |
| **等待付款** | 顯示金額 + 付款 QR Code | 條件確認後 |
| **等待旋鈕** | 大字顯示「請轉動旋鈕！」動畫提示 | 付款成功 |
| **開獎動畫** | 扭蛋機動畫 → 揭曉中獎艙位與商品名 | ESP32 觸發成功 |
| **取餐提示** | 顯示「第 N 號門已開，請取餐」 | 動畫播放完畢 |
| **錯誤畫面** | 顯示錯誤訊息 + 客服電話 | API 錯誤 / EMQX 失敗 |

### 8.3 Polling 策略

```javascript
// 狀態驅動的自適應 Polling
const POLL_INTERVALS = {
    'IDLE':                  5000,   // 5 秒
    'WAITING_FOR_TRIGGER':   500,    // 500ms（高頻，等旋鈕）
    'DISPENSING':            1000,   // 1 秒
};
```

### 8.4 動畫需求

- **扭蛋開獎動畫**：模擬扭蛋機旋轉 → 球掉出 → 打開揭曉，約 3~5 秒
- **使用純 CSS 動畫 + JavaScript 控制**，不依賴第三方動畫框架
- **艙位亮燈效果**：中獎艙位對應的 UI 格子閃爍高亮

---

## 9. 前端規格 — 手機端 (Mobile Web)

### 9.1 路由結構（Hash Router）

```
#/                      → 首頁（角色選擇 or 自動跳轉）
#/login                 → 登入
#/register              → 註冊

--- 消費者模式 ---
#/consumer/map          → 惜食地圖（Google Maps + 附近商品標記）
#/consumer/store/:id    → 店家詳情 + 商品列表
#/consumer/pay/:orderId → 付款頁（Mock）
#/consumer/orders       → 我的訂單
#/consumer/points       → 我的點數

--- 店家模式 ---
#/store/dashboard       → 店家主頁（今日上架統計）
#/store/add-item        → 上架新商品
#/store/pair            → 掃描 QR Code 配對機台
#/store/machine/:id     → 機台艙位管理
#/store/orders          → 訂單管理（核銷用）
```

### 9.2 設計原則

- **Mobile First**：所有頁面以手機寬度為基準設計
- **漸變色主題**：綠色系（環保/惜食意象）搭配暖色點綴
- **底部導航列**：消費者有「地圖 / 訂單 / 點數 / 我的」四個 Tab
- **店家導航列**：「主頁 / 上架 / 機台 / 訂單」四個 Tab

### 9.3 關鍵頁面規格

#### 惜食地圖頁

- 嵌入 Google Maps JavaScript API
- 以使用者當前位置為中心
- 商品圖標以 category 分色（便當=橙、麵包=黃、蔬菜=綠）
- 點擊標記顯示簡要商品資訊卡片
- 支援搜尋半徑調整（預設 3km）

#### 店家上架頁

- 表單欄位：商品名、分類（下拉）、原價、惜食價、過敏原（勾選）、有效期限
- 上架來源選擇：「放到平台地圖」或「放入機台」
- 選擇「放入機台」時，需先完成機台配對

#### 付款頁（Mock）

- 顯示商品/訂單資訊
- 一鍵「模擬付款」按鈕
- 付款後顯示取餐碼（路徑一）或「請至機台轉旋鈕」（路徑二）

### 9.4 第三方依賴（CDN 載入）

| 功能 | 套件 | 載入方式 |
|------|------|----------|
| QR Code 產生 | `qrcode.js` 或 `qr-code-styling` | CDN `<script>` |
| QR Code 掃描 | `html5-qrcode` | CDN `<script>` |
| 地圖 | Google Maps JavaScript API | `<script>` API Key |

---

## 10. 核心業務流程

### 10.1 路徑一：地圖導購完整流程

```
店家手機                    後端 API                    消費者手機
   │                          │                           │
   │  POST /products          │                           │
   │  (上架惜食品)        ──►│                           │
   │                          │  寫入 DB                  │
   │                          │◄──                        │
   │                          │                           │
   │                          │   GET /products?lat&lng   │
   │                          │◄──────────────────────────│
   │                          │                           │
   │                          │   商品列表 + 地圖標記     │
   │                          │──────────────────────────►│
   │                          │                           │
   │                          │   POST /orders/map        │
   │                          │◄──────────────────────────│
   │                          │                           │
   │                          │   Mock 付款成功            │
   │                          │   回傳取餐碼 A55B         │
   │                          │──────────────────────────►│
   │                          │                           │
   │                          │   消費者到店出示取餐碼     │
   │  POST /orders/:id/       │                           │
   │  complete                │                           │
   │  (輸入取餐碼核銷)    ──►│                           │
   │                          │   更新訂單 COMPLETED      │
   │                          │   發放點數                │
```

### 10.2 路徑二：扭蛋機台完整流程

```
平板 Kiosk          後端 API           ESP32          EMQX         消費者手機
   │                  │                  │              │              │
   │ [消費者操作平板]  │                  │              │              │
   │                  │                  │              │              │
   │  選擇過敏原排除  │                  │              │              │
   │  POST /gacha/    │                  │              │              │
   │  start       ──►│                  │              │              │
   │                  │  計算子池均價     │              │              │
   │  ◄── 回傳均價   │  檢查池非空      │              │              │
   │      & 付款連結  │                  │              │              │
   │                  │                  │              │              │
   │ [顯示付款 QR]    │                  │              │              │
   │                  │                  │              │    [掃碼付款] │
   │                  │  POST /gacha/pay │              │◄─────────────│
   │                  │◄─────────────────┼──────────────┼──────────────│
   │                  │                  │              │              │
   │                  │  訂單→WAITING    │              │              │
   │                  │  機台→WAITING    │              │              │
   │                  │                  │              │              │
   │ [Polling 偵測到  │                  │              │              │
   │  WAITING 狀態]   │                  │              │              │
   │ 顯示「請轉旋鈕」│                  │              │              │
   │                  │                  │              │              │
   │                  │  POST /trigger   │              │              │
   │                  │◄─────────────────│ [旋鈕轉動]  │              │
   │                  │                  │              │              │
   │                  │  抽獎演算法      │              │              │
   │                  │  抽中 4 號門     │              │              │
   │                  │                  │              │              │
   │                  │  HTTP POST ──────┼─────────────►│              │
   │                  │  (發布 MQTT)     │              │              │
   │                  │                  │              │              │
   │                  │                  │ MQTT OPEN    │              │
   │                  │                  │◄─────────────│              │
   │                  │                  │              │              │
   │                  │                  │ [開門+3s復位]│              │
   │                  │                  │              │              │
   │ [Polling 500ms]  │                  │              │              │
   │ GET /latest-     │                  │              │              │
   │ result       ──►│                  │              │              │
   │ ◄── 中獎資訊    │                  │              │              │
   │                  │                  │              │              │
   │ [播放開獎動畫]   │                  │              │              │
   │ [顯示取餐提示]   │                  │              │    [點數增加] │
   │                  │──────────────────┼──────────────┼─────────────►│
```

---

## 11. 安全與防呆機制

### 11.1 交易安全

| 機制 | 實作方式 |
|------|----------|
| **樂觀鎖** | 所有訂單狀態變更使用 `UPDATE ... WHERE version = ? AND status = ?`，若影響 0 列則拒絕 |
| **單槽模型** | 機台 `active_order_id` 非 NULL 時，拒絕新的扭蛋請求（回傳 409） |
| **庫存預留** | 付款成功時，對應艙位狀態從 `STOCKED` → `RESERVED`，確保不超賣 |
| **超時退款** | `WAITING_FOR_TRIGGER` 超過 5 分鐘自動退款並釋放庫存 |
| **冪等觸發** | ESP32 重複觸發時，若訂單已 `COMPLETED`，回傳 403 而非重複開門 |

### 11.2 認證安全

| 機制 | 實作方式 |
|------|----------|
| **密碼儲存** | bcrypt hash（cost factor 10） |
| **JWT** | HS256 簽章，有效期 24 小時 |
| **ESP32 認證** | 靜態 `secret_key`（MVP 階段接受此風險） |
| **配對 Token** | 隨機 UUID，10 分鐘有效期，單次使用 |

### 11.3 輸入驗證

- 所有 API 輸入需經過驗證（型別、長度、範圍）
- 過敏原只接受預定義清單內的值
- 金額不可為負數
- Email 格式驗證

---

## 12. 未來擴充

### 12.1 下一版本（v2.0）

- 🐾 **虛擬寵物養成系統**：消耗點數餵養、升級、裝飾
- 💳 **真實金流串接**：替換 Mock 為 ECPay / LINE Pay
- 📱 **推播通知**：訂單狀態變更推播
- 📊 **管理後台**：營運數據儀表板、機台監控

### 12.2 架構遷移（Cloudflare 部署）

- `server.js` → `worker.js`（Workers 入口）
- `better-sqlite3` → `D1 binding`
- MQTT mock → EMQX Serverless HTTP API
- 靜態檔案 → Cloudflare Pages
- 超時排程 → Workers Cron Trigger

---

## 附錄 A：過敏原代碼對照表

| 代碼 | 中文 | 英文 |
|------|------|------|
| `pork` | 豬肉 | Pork |
| `beef` | 牛肉 | Beef |
| `chicken` | 雞肉 | Chicken |
| `duck` | 鴨肉 | Duck |
| `lamb` | 羊肉 | Lamb |
| `seafood` | 海鮮 | Seafood |
| `egg` | 蛋 | Egg |
| `milk` | 乳製品 | Milk |
| `peanut` | 花生 | Peanut |
| `treenut` | 堅果 | Tree Nut |
| `wheat` | 小麥/麩質 | Wheat/Gluten |
| `soy` | 大豆 | Soy |
| `sesame` | 芝麻 | Sesame |

## 附錄 B：環境變數

```env
# === 伺服器 ===
PORT=3000
NODE_ENV=development

# === JWT ===
JWT_SECRET=your-super-secret-key-change-in-production

# === 資料庫（本地） ===
DB_PATH=./data/foodd.db

# === MQTT ===
MQTT_MODE=mock                    # mock | live
EMQX_API_URL=https://xxx.emqxsl.com:8443/api/v5
EMQX_API_KEY=your-emqx-api-key
EMQX_API_SECRET=your-emqx-api-secret

# === Google Maps ===
GOOGLE_MAPS_API_KEY=your-google-maps-key

# === 業務設定 ===
GACHA_TIMEOUT_MINUTES=5           # 旋鈕等待超時（分鐘）
POINTS_PER_DOLLAR=1               # 每消費 1 元得 1 點
PAIR_TOKEN_EXPIRE_MINUTES=10      # 配對 Token 有效期
```
