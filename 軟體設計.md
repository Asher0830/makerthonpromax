### 分成四個單位
#### 平板(惜食機器單純顯示) 機器 server 手機端


我們要做一個惜食整合系統，這個系統會有軟體以及硬體，我們不只有便當，還會有一些蔬菜或者是麵包。我們只要分成兩條路，第一個店家關門前，快賣完的時候可以直接透過平台到店家購買，這個是純軟體的，然後結合地圖這樣；第二個是店家要走了，把食物放在我們設計的惜食機台，這個機台有寫感測器這樣。然後我們這個機台有個特點，我們結合扭蛋抽獎機制，還有會員機制，每次購買都會集點。然後店家只要透過店家端用手機登錄商品，要簡單輸入商品名以及過敏原之類的，兩條路的系統邏輯不一樣，一個是掃描，登錄在網路上，然後客戶可以透過地圖，找到店家購買，然後可以集點；另一個是店家到機台用手機掃描商品(可能透過平板跳出QRcode連接到平台等等之類的)，那個門會打開可以放進去，那個門(機台)是esp32控制的，然後esp32也會監控商品狀態(DHT11)。當客人來的時候，透過平板來操控，然後登錄會員之類的，然後付款這樣，我們有個有趣的抽獎系統，透過ESP32上面旋鈕可以模擬扭蛋一樣，抽出商品，當然抽之前會簡單分類以及不吃什麼肉等等，每次扭蛋價格是透過可選商品的平均價。客人點數可以在網站有個寵物養成系統遊戲。


# 惜食整合系統 - 系統架構與技術規格書 (Serverless 敏捷開發版)



## 1. 專案概述
本專案為一款結合 O2O（線上到線下）地圖導購與 IoT 實體盲盒機台的惜食救援系統。針對敏捷開發與零預算需求，系統後端採用全 Serverless（無伺服器）架構，前端全面網頁化（Web App/PWA），結合硬體端 ESP32，實現從線上付款到線下自動開門取餐的完整閉環。

## 2. 系統架構：四大核心單位

本系統採軟硬體解耦設計，分為以下四個獨立單位：

### 2.1 雲端無伺服器後端 (Serverless Backend)
放棄傳統實體主機，改用雲端邊緣運算服務，達成零維護成本與高併發承受力。
* **核心 API 邏輯 (Cloudflare Workers)**：負責處理手機與平板的 HTTP 請求、執行盲盒隨機演算法、驗證付款狀態。
* **資料庫 (Cloudflare D1)**：Serverless SQLite 資料庫，儲存商品清單、各艙位狀態、過敏原標籤與會員點數。
* **硬體通訊橋樑 (EMQX Serverless 免費版)**：作為公有 MQTT Broker。Cloudflare Workers 抽出結果後，透過 HTTP 呼叫 EMQX 的 API，將開門指令轉為 MQTT 訊號下發給硬體。

### 2.2 機台硬體端 (ESP32 邊緣節點)
負責實體環境控制與觸發抽獎事件。
* **硬體觸發抽獎 (HTTP Client)**：消費者轉動實體旋鈕，ESP32 偵測到動作並經過硬體/軟體 Debounce（防彈跳）處理後，直接發送 HTTPS POST 請求給 Cloudflare Workers 觸發抽籤。
* **門鎖執行 (MQTT Subscriber)**：訂閱 EMQX 的控制主題，收到指令後輸出 PWM 驅動指定艙位的 SG90 伺服馬達解鎖。
* **溫控防護 (DHT11 + 繼電器)**：讀取溫濕度，程式碼內建「冷藏/加熱」繼電器互斥鎖，避免同時通電燒毀機台。

### 2.3 平板端 (Tablet Kiosk Web)
掛載於機台上的展示介面，使用網頁瀏覽器全螢幕運行。
* **消費者介面**：顯示盲盒平均價、提供過敏原剔除選項。
* **狀態同步**：透過輪詢 (Polling) 或 WebSocket 接收 Server 狀態，當 ESP32 觸發抽獎後，平板同步播放「扭蛋開獎特效動畫」。
* **店家配對 (URL Deep Linking)**：產生帶有 Session Token 的網址 QR Code（例：`https://app.domain.com/pair?token=123`），確保店家手機一掃即開網頁，避開瀏覽器相機權限的卡頓問題。

### 2.4 手機端 (Mobile Web / PWA)
整合店家與消費者的雙身分入口，免下載直接使用。
* **消費者模式**：整合 Google Maps API 顯示剩食地圖，支援線上付款；內建「虛擬寵物系統」，消費者可消耗購買獲得的點數進行餵養與升級。
* **店家模式**：快速登錄快過期商品。透過掃描平板的 URL QR Code，自動與機台建立配對，進行艙位分配與放餐。

---

## 3. 核心業務流程 (Sequence Flow)

### 3.1 路徑一：純軟體地圖導購
1. **上架**：店家透過手機 Web 送出商品資訊至 Cloudflare Workers，寫入 D1 資料庫。
2. **查詢**：消費者手機 Web 獲取附近店家地圖。
3. **交易**：消費者線上付款，系統扣除庫存並發放寵物點數。
4. **核銷**：消費者至實體店面出示數位憑證完成取餐。

### 3.2 路徑二：實體機台盲盒 (硬體驅動抽獎流)

1. **條件設定與付款**：
   * 消費者於平板 Web 選擇「便當池」並剔除不吃的肉類。
   * 平板顯示付款連結，消費者手機掃碼付款。
   * Cloudflare 驗證金流成功，將該次交易狀態標記為 `WAITING_FOR_TRIGGER`，平板顯示「請轉動實體旋鈕」。

2. **硬體實體觸發**：
   * 消費者轉動機台旋鈕。
   * ESP32 偵測到轉動，發送 HTTP POST (`/api/trigger-gacha`) 至 Cloudflare Workers。

3. **伺服器結算與指令下發**：
   * Cloudflare Workers 收到觸發，計算該池剩餘商品平均價：
     $$P_{\text{avg}} = \frac{1}{n} \sum_{i=1}^{n} P_i$$
   * 剔除過敏原後進行亂數抽籤（例：抽中 4 號門）。
   * Workers 發送 HTTP POST 至 EMQX REST API，要求轉發 `OPEN` 指令給 4 號門。

4. **取餐與遊戲化**：
   * EMQX 將指令透過 MQTT 推送至 ESP32。
   * ESP32 驅動 4 號門的 SG90 彈開，消費者取餐。
   * 平板 Web 抓取到最新狀態，播放 4 號門中獎動畫。
   * 消費者手機 Web 點數增加，可進入寵物系統進行互動。

---

## 4. 通訊協定與 API / MQTT Payload 規範

### 4.1 ESP32 觸發抽籤 (HTTP POST: ESP32 ➔ Cloudflare Workers)
* **Endpoint**: `https://api.yourdomain.com/v1/machine/trigger`
* **Payload (JSON)**:
```json
{
  "machine_id": "MAC_01A2B3",
  "action": "KNOB_TURNED",
  "secret_key": "esp32_auth_token"
}
```
### 4.2 伺服器要求 EMQX 轉發開門 (HTTP POST: Cloudflare ➔ EMQX)

* **Endpoint**: `https://{emqx-cloud-address}/api/v5/publish`
* **Payload (JSON)**:

```json
{
  "topic": "v1/machines/MAC_01A2B3/control",
  "qos": 1,
  "payload": "{\"door_index\": 4, \"action\": \"OPEN\", \"pwm_ms\": 1500}"
}

```

### 4.3 ESP32 接收開門指令 (MQTT 訂閱: EMQX ➔ ESP32)

* **Topic**: `v1/machines/MAC_01A2B3/control`
* **Payload 解析**: ESP32 解析 JSON，針對第 4 個 SG90 輸出 1500ms 的 PWM 訊號，延遲 3 秒後自動復位。

### 4.4 環境與溫控遙測 (MQTT 發布: ESP32 ➔ EMQX ➔ Cloudflare Webhook, 每 1 分鐘)

* **Topic**: `v1/machines/MAC_01A2B3/telemetry`
* **Payload**:

```json
{
  "temperature": 15.2,
  "humidity": 60.5,
  "hardware_status": {
    "cooling_relay": true,
    "heating_relay": false
  }
}

```


## 5. 系統限制與防呆機制

1. **實體雜訊過濾 (Debounce)**：ESP32 程式碼內必須實作至少 500ms 的軟體防彈跳，確保使用者快速轉動旋鈕時，不會發出海量的 HTTP 請求導致 Cloudflare 阻擋或重複抽獎。
2. **交易鎖定 (Transaction Lock)**：Cloudflare 資料庫必須採用狀態機邏輯。一筆訂單對應一次抽獎，一旦抽出結果，該筆訂單狀態即鎖定為 `COMPLETED`。即便 ESP32 異常連續送出觸發訊號，API 也應直接回傳 `403 Forbidden`。
3. **溫控互斥防護**：ESP32 腳位控制必須寫死 `if (heating == HIGH) { cooling = LOW; }`，將安全邏輯鎖在最底層硬體，絕不依賴雲端的控制訊號來避免短路。
